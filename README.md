# useful aliases
co =checkout -q

ci =commit -q -m

cie=commit --allow-empty -q -m

lg =log --oneline --decorate --graph

br =branch

st =status -s -b

cl =clean -df

ps =push origin --tags

pl =pull
